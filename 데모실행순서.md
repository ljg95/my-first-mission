# 데모 실행 순서 (라이브 시연용)

> 발표하면서 **한 줄씩 복사해 터미널에 붙여넣고 실행**하면 됩니다.
> 슬라이드 5·6·7 에서 각각 STEP 1~2 / STEP 3 / STEP 4 를 실행합니다.
>
> - **Windows(PowerShell)** 로 발표 → 아래 STEP 0~5 를 그대로 사용
> - **Mac / Linux(bash·zsh)** 로 발표 → 맨 아래 **"Mac / Linux (bash·zsh) 전체 버전"** 섹션을 사용

---

## ⚠️ 발표 전 반드시 확인 (중요)

라이브 데모는 `python:3.12-slim`, `busybox` 이미지가 **로컬에 미리 받아져 있어야** 합니다.
발표장 네트워크에서 이미지를 못 받는 경우(프록시/방화벽/인증서 차단)가 많으니,
**인터넷 되는 곳에서 미리 아래를 실행해 이미지를 캐시**해 두세요:

```powershell
docker pull python:3.12-slim
docker pull busybox
docker build -t myfirstmission-web .
```

확인 (세 개가 다 보이면 준비 완료):

```powershell
docker images --format '{{.Repository}}:{{.Tag}}' | Select-String 'python|busybox|myfirstmission'
```

> **캐시가 없고 네트워크도 막혀 있으면** 라이브 데모 대신,
> 슬라이드 5·6·7에 이미 적혀 있는 명령/출력(200 OK, volume-test 등)을 그대로 읽으며
> 설명하는 방식으로 진행하세요. README.md에 전체 실행 로그가 증거로 남아 있습니다.

---

## STEP 0. 시작 전 준비 (발표 직전에 미리)

프로젝트 루트로 이동하고 Docker가 켜져 있는지 확인:

```powershell
cd C:\my-first-mission
docker version --format '{{.Server.Version}}'
```

발표 슬라이드 띄우기 (별도 창/브라우저):

```powershell
node .claude\serve.js 8092
```
→ 브라우저에서 `http://localhost:8092/` 접속 후 `F` 로 전체화면.

---

## STEP 1. 이미지 빌드  (슬라이드 5)

```powershell
docker build -t myfirstmission-web .
```

## STEP 2. 실행 + 접속 확인  (슬라이드 5)

컨테이너 실행:

```powershell
docker run --rm -d -p 8080:8080 --name mfm-web myfirstmission-web
```

접속 확인 (200 OK 가 떨어지는지):

```powershell
curl.exe -I http://localhost:8080
```

브라우저로도 한 번 보여주기 → `http://localhost:8080`

---

## STEP 3. Bind Mount 실시간 반영  (슬라이드 6)

호스트 `app` 폴더를 컨테이너에 직접 연결해서 실행:

```powershell
docker run --rm -d -p 8081:8080 -v "${PWD}\app:/usr/src/app" -w /usr/src/app --name mfm-bind python:3.12-slim python3 -m http.server 8080
```

지금 내용 확인:

```powershell
curl.exe -s http://localhost:8081 | Select-String "8080"
```

**호스트에서 파일을 수정** (예: 문구 하나 바꾸기):

```powershell
(Get-Content app\index.html) -replace '포트 <strong>8080</strong>', '포트 <strong>8080</strong> (LIVE 수정됨)' | Set-Content app\index.html
```

**재빌드 없이** 바로 반영되는지 다시 확인:

```powershell
curl.exe -s http://localhost:8081 | Select-String "LIVE 수정됨"
```
→ 방금 고친 문구가 그대로 나오면 성공. (원복: `git checkout app/index.html`)

---

## STEP 4. Volume 데이터 영속성  (슬라이드 7)

볼륨 생성:

```powershell
docker volume create myfirstmission-data
```

볼륨에 데이터 쓰기:

```powershell
docker run --rm -v myfirstmission-data:/data busybox sh -c "echo volume-test > /data/volume.txt && cat /data/volume.txt"
```

**컨테이너를 삭제(--rm)한 뒤 새 컨테이너로 다시 읽기** — 그래도 데이터가 남아있음:

```powershell
docker run --rm -v myfirstmission-data:/data busybox cat /data/volume.txt
```
→ `volume-test` 가 그대로 출력되면 영속성 검증 성공.

---

## STEP 5. 발표 끝나고 정리 (뒷정리)

```powershell
docker rm -f mfm-web mfm-bind 2>$null
docker volume rm myfirstmission-data 2>$null
git checkout app/index.html
```

---

## Mac에 Docker 설치하기 (처음이라면 여기부터)

Mac은 Docker가 기본 설치돼 있지 않습니다. **Docker Desktop** 을 설치하면 됩니다.
> 참고: 설치 후에는 `docker` 명령에 **sudo 가 필요 없습니다.** (Mac Docker Desktop은 유저 권한으로 동작)
> 단, **설치 자체는 관리자(admin) 권한이 한 번 필요**합니다.

### 1) 내 Mac 칩 확인

터미널에서:

```bash
uname -m
```
- `arm64` → **Apple Silicon** (M1/M2/M3/M4)
- `x86_64` → **Intel**

### 2) Docker Desktop 설치 — 방법 A: 공식 설치파일 (가장 쉬움)

1. https://www.docker.com/products/docker-desktop/ 접속
2. 칩에 맞는 버전 다운로드 — "Download for Mac – Apple Silicon" 또는 "Intel Chip"
3. 받은 `Docker.dmg` 더블클릭 → **Docker 아이콘을 Applications 폴더로 드래그**
4. `Applications` 에서 **Docker** 실행 → 최초 실행 시 권한 요청이 뜨면 관리자 비밀번호 입력
5. 상단 메뉴바에 고래 아이콘이 "Docker Desktop is running" 상태가 되면 준비 완료

### 2) 방법 C: OrbStack (가볍고 빠름 · Mac 추천) ⭐

Docker Desktop 대안. 훨씬 가볍고 빠르며 `docker` CLI 가 그대로 호환됩니다.
**아래 STEP 명령들을 하나도 안 바꾸고** 그대로 쓸 수 있습니다.

1. https://orbstack.dev 에서 다운로드 (또는 `brew install orbstack`)
2. OrbStack 앱 실행 → 첫 실행 시 자동 설정 (이때 **관리자 비밀번호 한 번** 입력 필요)
3. 설정이 끝나면 `docker` 명령이 바로 동작 — 별도 context 설정 불필요
4. 확인:

```bash
docker run --rm hello-world
```

> OrbStack 도 최초 설정 때 관리자 비밀번호를 한 번 요구합니다(네트워크 헬퍼 설치).
> 완전히 admin 권한이 없다면 이것도 설치가 막힐 수 있으니, 그 경우엔 IT 요청 또는 데모 없이 발표하세요.
> 개인/비영리는 무료입니다.

### 2) 방법 B: Homebrew 로 설치 (터미널 선호 시)

```bash
brew install --cask docker
```
설치 후 `Applications` 에서 **Docker** 앱을 한 번 실행해 엔진을 켜야 합니다.
> Homebrew 가 없다면 먼저 https://brew.sh 의 설치 명령을 실행 (이때 관리자 권한 필요).

### 3) 설치 확인

```bash
docker version
docker run --rm hello-world
```
`Hello from Docker!` 가 나오면 정상. 이제 위 STEP 들을 실행할 수 있습니다.

> **관리자 권한이 없다면?** (회사/학교 Mac 등)
> Docker Desktop 설치는 admin 이 반드시 필요합니다. 직접 설치가 안 되면
> ① IT/관리자에게 설치 요청하거나, ② 라이브 데모를 포기하고 슬라이드의 명령·출력을
> 읽으며 설명하는 방식(README.md 로그가 증거)으로 진행하세요.

---

## Mac / Linux (bash·zsh) 전체 버전

> Mac(또는 Linux)에서 발표한다면 위 STEP 대신 **이 섹션을 그대로 위→아래로** 따라 하세요.
> Docker Desktop(Mac)이 실행 중이어야 합니다.

### (사전) 이미지 미리 받기 — 인터넷 되는 곳에서

```bash
docker pull python:3.12-slim
docker pull busybox
docker build -t myfirstmission-web .
docker images --format '{{.Repository}}:{{.Tag}}' | grep -E 'python|busybox|myfirstmission'
```

### STEP 0. 준비

```bash
cd ~/my-first-mission          # ← 실제 프로젝트 경로로 바꾸세요
docker version --format '{{.Server.Version}}'
node .claude/serve.js 8092     # 슬라이드 서버 → 브라우저 http://localhost:8092/ 접속 후 F 로 전체화면
```

### STEP 1~2. 빌드 · 실행 · 접속 (슬라이드 5)

```bash
docker build -t myfirstmission-web .
docker run --rm -d -p 8080:8080 --name mfm-web myfirstmission-web
curl -I http://localhost:8080
```
→ 브라우저로 `http://localhost:8080` 도 보여주기 (200 OK 확인).

### STEP 3. Bind Mount 실시간 반영 (슬라이드 6)

```bash
docker run --rm -d -p 8081:8080 -v "$PWD/app":/usr/src/app -w /usr/src/app --name mfm-bind python:3.12-slim python3 -m http.server 8080
```

호스트에서 파일 수정 (재빌드 없이 반영되는 걸 보여주는 게 핵심):

```bash
perl -pi -e 's{포트 <strong>8080</strong>}{포트 <strong>8080</strong> (LIVE 수정됨)}' app/index.html
```

바로 반영 확인:

```bash
curl -s http://localhost:8081 | grep "LIVE 수정됨"
```
→ 방금 고친 문구가 나오면 성공. (원복: `git checkout app/index.html`)

### STEP 4. Volume 데이터 영속성 (슬라이드 7)

```bash
docker volume create myfirstmission-data
docker run --rm -v myfirstmission-data:/data busybox sh -c "echo volume-test > /data/volume.txt && cat /data/volume.txt"
docker run --rm -v myfirstmission-data:/data busybox cat /data/volume.txt
```
→ 컨테이너를 `--rm`으로 지운 뒤에도 `volume-test` 가 그대로 나오면 영속성 검증 성공.

### STEP 5. 뒷정리

```bash
docker rm -f mfm-web mfm-bind 2>/dev/null
docker volume rm myfirstmission-data 2>/dev/null
git checkout app/index.html
```

---

### PowerShell ↔ Mac(bash) 차이 요약

| 항목 | PowerShell | Mac / Linux (bash) |
|------|-----------|--------------------|
| Bind 마운트 경로 | `-v "${PWD}\app:/usr/src/app"` | `-v "$PWD/app":/usr/src/app` |
| curl | `curl.exe -I ...` | `curl -I ...` |
| 문자열 검색 | `... \| Select-String "..."` | `... \| grep "..."` |
| 파일 수정 | `(Get-Content ...) -replace ... \| Set-Content ...` | `perl -pi -e 's{...}{...}' ...` |
| null 버리기 | `2>$null` | `2>/dev/null` |
| 경로 구분자 | `\` (역슬래시) | `/` (슬래시) |

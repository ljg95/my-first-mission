# my-first-mission
## 터미널 조작 로그

### 1. 작업 디렉토리 생성
\`\`\`bash
$ pwd
/Users/사용자이름
$ mkdir my-dev-workstation && cd my-dev-workstation
$ mkdir app docs docs/screenshots
$ ls -R
app  docs
...
\`\`\`
## Git 설정
\`\`\`bash
$ git config --global user.name "..."
$ git config --global user.email "..."
$ git config --global init.defaultBranch main
\`\`\`
# Mac 발표 실행 명령어 (순서대로)

> OrbStack 확인 → Docker 이미지 빌드/실행 → **발표자료를 Docker 서버로 서빙** → 정리
> 터미널(zsh/bash)에서 **한 블록씩 복사해 실행**하세요. 기준 위치: 프로젝트 폴더 안.
> 슬라이드 이동: `←` `→` `Space` · 전체화면: `F`

---

## 0. 프로젝트 폴더로 이동

```bash
cd ~/my-first-mission     # ← 실제 경로로 바꾸세요
```

---

## 1. OrbStack 설치 확인

```bash
ls /Applications | grep -i orbstack
```
- `OrbStack.app` 이 나오면 → 설치됨. **2번으로**.
- 아무것도 안 나오면 → 미설치. 아래로 설치 후 진행.

**(미설치 시) 설치:**
```bash
brew install orbstack
```
> 또는 https://orbstack.dev 에서 다운로드. 설치 후 **OrbStack 앱을 한 번 실행**해야 엔진이 켜집니다.

---

## 2. Docker 엔진 켜기 + 확인

OrbStack(엔진) 실행:
```bash
open -a OrbStack
```

엔진이 살아있는지 확인 (서버 버전이 나오면 OK):
```bash
docker version --format '{{.Server.Version}}'
```

---

## 3. Docker 이미지 빌드

```bash
docker build -t myfirstmission-web .
```
> 실패 시(이미지 다운로드 차단): 인터넷 되는 곳에서 먼저 `docker pull python:3.12-slim` 후 다시 빌드.

---

## 4. 컨테이너 실행 (발표자료 서빙 시작)

이전 컨테이너가 있으면 정리:
```bash
docker rm -f mfm-web 2>/dev/null
```

컨테이너 실행 — 호스트 8080 → 컨테이너 8080:
```bash
docker run -d --rm -p 8080:8080 --name mfm-web myfirstmission-web
```

잘 떴는지 확인 (200 OK 확인):
```bash
curl -I http://localhost:8080/presentation.html
```

---

## 5. 발표자료 열기

```bash
open http://localhost:8080/presentation.html
```
> 브라우저가 열리면 `F` 로 전체화면. 이 페이지는 **방금 띄운 Docker 컨테이너가 직접 서빙**합니다.
> (슬라이드 3의 "지금 이 슬라이드도 이 컨테이너가 서빙 중!" 이 실제로 참이 됩니다.)

---

## 6. 발표 끝나고 정리

```bash
docker rm -f mfm-web 2>/dev/null
```
> `--rm` 옵션이라 종료되면 컨테이너는 자동 삭제됩니다. 이미지는 남아 다음에 바로 재사용됩니다.

---

## 한 번에 몰아 실행 (선택)

명령을 하나씩 안 하고 순서대로 이어붙여 실행하고 싶을 때:

```bash
cd ~/my-first-mission && \
open -a OrbStack && sleep 3 && \
docker build -t myfirstmission-web . && \
docker rm -f mfm-web 2>/dev/null; \
docker run -d --rm -p 8080:8080 --name mfm-web myfirstmission-web && \
sleep 2 && open http://localhost:8080/presentation.html
```

---

## 참고: 데모 슬라이드용 추가 명령 (Bind Mount / Volume)

슬라이드 6·7에서 라이브로 보여줄 데모는 `데모실행순서.md` 의 STEP 3·4 참고.
발표자료 자체는 위 1~5번만으로 충분히 실행됩니다.

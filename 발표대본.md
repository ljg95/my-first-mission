# My First Mission — 발표 대본

> 전체 9슬라이드 · 목표 시간 **6~8분** · 이동: `→` / `Space`, 전체화면 `F`
> 데모를 라이브로 보여줄 슬라이드에는 **[데모]** 표시가 있습니다. → `데모실행순서.md` 참고

---

## 슬라이드 1 — 표지 (약 20초)

> "안녕하세요, 이번에 진행한 첫 번째 미션 **My First Mission**을 발표하겠습니다.
> Docker로 웹 서버 컨테이너를 직접 만들고, bind mount와 볼륨으로 데이터를 다루고,
> Git/GitHub 연동까지 검증한 실습입니다."

- 여기서 잠깐 멈추고 청중을 한 번 본 뒤 `→` 로 넘어가기.

---

## 슬라이드 2 — 이번 미션의 목표 (약 40초)

> "이번 미션의 목표는 네 가지였습니다.
> 첫째, **Docker로 웹 서버 컨테이너를 직접 빌드**하는 것 — Dockerfile 작성부터 이미지, 실행까지.
> 둘째, **포트 매핑**으로 격리된 컨테이너를 호스트에서 접속하기.
> 셋째, **Bind Mount와 Volume**의 차이를 이해하고 데이터 전략을 세우는 것.
> 넷째, **Git과 GitHub 연동**으로 버전 관리와 협업 흐름을 경험하는 것입니다."

- 네 항목을 손가락으로 하나씩 짚듯이 또박또박.

---

## 슬라이드 3 — 한눈에 보는 구조 (약 40초)

> "전체 구조를 먼저 보겠습니다.
> 왼쪽은 **호스트, 제 PC**입니다. 브라우저와 app 폴더가 여기 있죠.
> 오른쪽은 **컨테이너**, python http.server가 도는 격리된 공간입니다.
> 이 둘을 잇는 다리가 바로 **포트 매핑** `-p 8080:8080` 이고,
> 데이터를 잇는 게 마운트와 볼륨입니다.
> 참고로 **지금 보시는 이 발표 화면도 그 컨테이너가 서빙**하고 있습니다."

- 마지막 문장은 살짝 웃으면서 — 청중 관심 끌기 포인트.

---

## 슬라이드 4 — Dockerfile, 단 6줄 (약 45초)

> "웹 서버 컨테이너는 이 **6줄짜리 Dockerfile**로 끝납니다.
> `FROM`으로 경량 파이썬 이미지를 베이스로 잡고,
> `WORKDIR`로 작업 폴더를 지정하고,
> `COPY`로 웹 파일을 넣고,
> `VOLUME`으로 영속 데이터 지점을 표시하고,
> `EXPOSE`로 포트를 명시한 뒤,
> `CMD`로 파이썬 내장 웹 서버를 실행합니다.
> **재료 → 조립 → 실행**, 이 순서로 읽으면 됩니다."

---

## 슬라이드 5 — 포트 매핑 접속 **[데모]** (약 60초)

> "그럼 실제로 띄워보겠습니다."

- **[데모 1 실행]** → `데모실행순서.md`의 STEP 1~2 (build → run → curl)
- 브라우저에서 `http://localhost:8080` 도 한 번 보여주기.

> "보시는 것처럼 `curl`에 **200 OK**가 떨어집니다.
> 컨테이너는 원래 격리된 네트워크 안에 있어서 밖에서 안 보이는데,
> `-p 8080:8080` 으로 호스트 포트와 이어줬기 때문에 접속이 되는 겁니다."

---

## 슬라이드 6 — Bind Mount, 실시간 반영 **[데모]** (약 60초)

> "다음은 **Bind Mount**입니다. 호스트 폴더를 컨테이너에 직접 연결하는 방식이죠."

- **[데모 2 실행]** → `데모실행순서.md`의 STEP 3 (bind mount 실행 → index.html 수정 → 다시 curl)

> "호스트에서 `index.html`을 고치고 다시 요청하면,
> **재빌드 없이 바로 반영**됩니다. 코드 고치자마자 결과가 보이니까
> **개발 단계**에서 특히 유용합니다."

---

## 슬라이드 7 — Volume, 데이터 영속성 **[데모]** (약 60초)

> "반대로 **Volume**은 Docker가 직접 관리하는 저장소입니다."

- **[데모 3 실행]** → `데모실행순서.md`의 STEP 4 (volume 생성 → 쓰기 → 컨테이너 삭제 후 다시 읽기)

> "여기 보시면 컨테이너를 `--rm`으로 **삭제한 뒤에 다시 읽어도** `volume-test` 값이 그대로 남아있습니다.
> Bind Mount가 개발용이라면, Volume은 **DB나 로그 같은 운영 데이터**를 안전하게 보관하는 용도입니다."

---

## 슬라이드 8 — 핵심 개념 정리 (약 50초)

> "핵심 개념을 네 가지로 정리했습니다.
> **Bind vs Volume**: Bind는 호스트 경로 직접 연결(개발·실시간), Volume은 Docker 관리(운영·영속).
> **포트 매핑**: `-p 호스트:컨테이너`, 격리된 서비스를 밖으로 노출하는 다리.
> **권한 755/644**: r은 4, w는 2, x는 1. 755는 소유자 rwx에 나머지는 r-x.
> **Git vs GitHub**: Git은 로컬 버전관리 도구, GitHub는 원격 협업 플랫폼입니다."

- 4개 박스라 빠르게, 청중이 읽을 시간을 2~3초 주기.

---

## 슬라이드 9 — 배운 것 & 마무리 (약 30초)

> "정리하면, 이미지에서 컨테이너, 그리고 서비스까지 **전체 흐름을 직접 손으로 경험**했고,
> 데이터를 언제 Bind로, 언제 Volume으로 다뤄야 하는지 이해했으며,
> 모든 검증을 **로그와 증거로 문서화**해서 재현 가능한 결과물을 남겼습니다.
> 이상입니다. 감사합니다. 질문 받겠습니다."

---

## 예상 질문 & 답변 (Q&A 대비)

- **Q. Bind Mount랑 Volume, 실무에선 뭘 써요?**
  A. 개발 중 코드 반영은 Bind, DB/업로드 파일 같은 영속 데이터는 Volume. 운영에선 대부분 Volume입니다.

- **Q. 포트 8080:8080에서 앞뒤 숫자가 뭐가 다른가요?**
  A. 앞이 호스트 포트, 뒤가 컨테이너 내부 포트입니다. `-p 9000:8080`이면 호스트 9000으로 접속해도 컨테이너 8080으로 전달됩니다.

- **Q. `--rm`이 뭔가요?**
  A. 컨테이너가 종료되면 자동 삭제하는 옵션입니다. 데모에서 "삭제 후에도 볼륨 데이터가 남는다"를 보여줄 때 사용했습니다.

- **Q. 왜 python http.server를 썼나요?**
  A. 별도 프레임워크 없이 파이썬 표준 라이브러리만으로 정적 웹 서버를 띄울 수 있어, 실습 목적에 가장 가볍고 빠릅니다.

- **Q. VOLUME을 Dockerfile에 선언한 것과 `-v`로 마운트하는 건 뭐가 다른가요?**
  A. Dockerfile의 `VOLUME`은 "이 경로는 영속 지점"이라고 표시하는 것이고, 실제 이름 붙은 볼륨 연결은 실행 시 `-v` 옵션으로 합니다.
